#!/bin/bash

# Simple test script to verify shell persistence works

echo "Testing shell persistence fix..."

# The tool agent should be able to:
# 1. Export a variable
# 2. Echo that variable in the same session
# 3. Run multiple commands sequentially

echo "Build complete. Now run: cargo run --release"
